<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book Reviews</title>
  <script src="https://cdn.tailwindcss.com"></script>
  
</head>

<body class="bg-gradient-to-b from-purple-500 to-teal-500">
  <header class="flex justify-between items-center p-2">
    
    
    {{#if logged_in}}
    <nav class="ml-auto">
      <a href="../homepage.handlebars" class="bg-sky-400 px-3 py-1 rounded-full mr-5 border border-purple-200 text-white
      hover:border-transparent focus:outline-none focus:ring-2 focus:ring-purple-600 focus:ring-offset-2">Homepage</a>
      <button class="btn bg-sky-400 px-3 py-1 rounded-full mr-5 border border-purple-200 text-white
      hover:border-transparent focus:outline-none focus:ring-2 focus:ring-purple-600 focus:ring-offset-2" id="logout">Logout</button>
    </nav>
    {{/if}}
  </header>
  <main class="container container-fluid mt-5">
    {{{ body }}}
  </main>
  <footer>
      
  </footer>

 
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>
